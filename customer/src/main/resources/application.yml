server:
  port: 8080


spring:
  application:
    name: customer
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost/customer
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
